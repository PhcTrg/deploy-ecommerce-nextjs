"use client";

import { useEffect, useState } from "react";
import Image from "next/image";
import cartAPIs from "@/api/cart";
import Link from "next/link";
import { useDisclosure } from "@nextui-org/react";
import ResultModal from "@/components/modal/result-modal";
import { useRouter } from "next/navigation";

const CartContent = () => {
  // ... paste all the content from the previous CartPage component here ...
  // (everything from const [isLoading, setIsLoading] to the end of the component)
};

export default CartContent;
